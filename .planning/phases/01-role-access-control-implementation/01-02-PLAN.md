---
phase: 01-role-access-control-implementation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - app/pages/admin/AdminUsers.tsx
  - app/pages/admin/AdminUserEdit.tsx
  - app/pages/admin/AdminCreateUser.tsx
  - app/pages/admin/AdminDashboard.tsx
  - app/pages/admin/AdminCategories.tsx
  - app/pages/admin/AdminPaths.tsx
  - app/pages/admin/AdminPathDetail.tsx
  - app/pages/CourseBuilder.tsx
  - app/pages/CourseBuilderLesson.tsx
  - app/layouts/MainLayout.tsx
  - app/pages/IndexRedirect.tsx
  - app/pages/Challenges.tsx
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "Every former `user.role !== 'staff'` admin guard (including AdminUserEdit.tsx:51, AdminCreateUser.tsx:34/:52) now calls `can(user, 'admin')` and preserves its original redirect target (AdminUserEdit and AdminCreateUser redirect to /dashboard)."
    - "The canonical client-side staff nav gate in `app/layouts/MainLayout.tsx:188` is migrated from `{user.role === 'staff' && (...)}` to `{user && can(user,'admin') && (...)}` (import `can` from `~/auth/permissions`)."
    - "AdminUserEdit and AdminCreateUser role zod enums accept the full `USER_ROLES` vocabulary (learner/lite/pro/staff), fixing the latent bug that rejected lite/pro."
    - "Challenges.tsx uses `can(user, 'accessChallenges')` in both loader and action; the `canAccessChallenges` import from access.ts is gone."
    - "IndexRedirect routes staff (`can(user,'admin')`) to /admin and everyone else to /dashboard."
    - "NO `user.role === 'staff'` OR `user.role !== 'staff'` literal remains anywhere in app/pages, app/middleware, OR app/layouts. Display-only badges use a different identifier (`u.role === 'staff'`) and are explicitly out of scope."
  artifacts: []
  key_links:
    - "Each migrated guard keeps the existing `NoUserContextError` for null user, THEN `can()` for the role check (per PATTERNS null-user convention)."
    - "Display-only `u.role === 'staff'` badge renders (e.g. AdminUsers line 168, AdminDashboard line 435) are NOT permission gates — leave untouched."
---

<objective>
Migrate all remaining page-level scattered role checks to `can()` (D-04 full migration). This is Wave 2 — depends on Wave 1's shared `can()` existing. Covers 8 admin pages, CourseBuilder (+Lesson), IndexRedirect, and Challenges, plus the two admin role zod-enum reconciliations.

Purpose: Complete the centralization — remove the last `user.role !== 'staff'` literals and the `canAccessChallenges` import so `can()` is the single source of truth.
Output: All listed page files migrated; typecheck + vitest green.
</objective>

<execution_context>
@/home/tkl/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/tkl/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-role-access-control-implementation/01-PATTERNS.md
@/var/my-data/fpt/exe201/web-app/app/auth/permissions.ts
@/var/my-data/fpt/exe201/web-app/app/pages/IndexRedirect.tsx
@/var/my-data/fpt/exe201/web-app/app/pages/Challenges.tsx
@/var/my-data/fpt/exe201/web-app/app/pages/CourseBuilder.tsx
@/var/my-data/fpt/exe201/web-app/app/pages/CourseBuilderLesson.tsx
@/var/my-data/fpt/exe201/web-app/app/pages/admin/AdminUsers.tsx
@/var/my-data/fpt/exe201/web-app/app/pages/admin/AdminUserEdit.tsx
@/var/my-data/fpt/exe201/web-app/app/pages/admin/AdminCreateUser.tsx
@/var/my-data/fpt/exe201/web-app/app/pages/admin/AdminDashboard.tsx
@/var/my-data/fpt/exe201/web-app/app/pages/admin/AdminCategories.tsx
@/var/my-data/fpt/exe201/web-app/app/pages/admin/AdminPaths.tsx
@/var/my-data/fpt/exe201/web-app/app/pages/admin/AdminPathDetail.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate admin page + layout guards to can(user,'admin') (D-04)</name>
  <files>app/pages/admin/AdminUsers.tsx, app/pages/admin/AdminDashboard.tsx, app/pages/admin/AdminCategories.tsx, app/pages/admin/AdminPaths.tsx, app/pages/admin/AdminPathDetail.tsx, app/pages/CourseBuilder.tsx, app/pages/CourseBuilderLesson.tsx, app/pages/admin/AdminUserEdit.tsx, app/pages/admin/AdminCreateUser.tsx, app/layouts/MainLayout.tsx</files>
  <read_first>app/auth/permissions.ts, app/pages/admin/AdminUsers.tsx, app/pages/CourseBuilder.tsx, app/pages/CourseBuilderLesson.tsx, app/pages/admin/AdminUserEdit.tsx, app/pages/admin/AdminCreateUser.tsx, app/layouts/MainLayout.tsx</read_first>
  <action>
    For EACH file below, add `import { can } from '~/auth/permissions'` (top of file, near other imports) and replace the admin guard literal `if (user.role !== 'staff') {` with `if (!can(user, 'admin')) {`. PRESERVE the existing `NoUserContextError` block that runs BEFORE the role check (null guard stays), and PRESERVE the existing `throw redirect(...)` target.

    Exact sites (verified line numbers):
    - `app/pages/admin/AdminUsers.tsx`: lines 37 and 73 (both `throw redirect('/')`)
    - `app/pages/admin/AdminDashboard.tsx`: line 128 (`throw redirect('/')`)
    - `app/pages/admin/AdminCategories.tsx`: lines 36 and 56 (both `throw redirect('/')`)
    - `app/pages/admin/AdminPaths.tsx`: lines 49 and 76 (both `throw redirect('/')`)
    - `app/pages/admin/AdminPathDetail.tsx`: lines 48 and 96 (both `throw redirect('/')`)
    - `app/pages/CourseBuilder.tsx`: inside `requireStaffUser` helper, line 154 (`throw redirect('/')`) — change ONLY that one line; keep the function signature and null check.
    - `app/pages/CourseBuilderLesson.tsx`: line 105 (`throw redirect('/')`)
    - `app/pages/admin/AdminUserEdit.tsx`: line 51 — `if (user.role !== 'staff') {` guards the page; PRESERVE the existing `NoUserContextError` block before it and PRESERVE its `throw redirect('/dashboard')` target. Replace with `if (!can(user, 'admin')) {`.
    - `app/pages/admin/AdminCreateUser.tsx`: lines 34 and 52 — both `if (user.role !== 'staff') {` guards; PRESERVE the existing `NoUserContextError` blocks before each and PRESERVE their `throw redirect('/dashboard')` target. Replace with `if (!can(user, 'admin')) {`.

    `app/layouts/MainLayout.tsx`:
    - Add `import { can } from '~/auth/permissions'` (top of file, near other imports).
    - Line 188: replace the staff nav gate `{user.role === 'staff' && (...staff nav...)}` with `{user && can(user, 'admin') && (...staff nav...)}`. This is the canonical client-side use case (render-gated nav, not a redirect) — keep the same inner nav JSX.

    IMPORTANT: Do NOT touch display-only badge renders such as `u.role === 'staff'` in AdminUsers (~line 168) or AdminDashboard (~line 435) — those color a label, not a gate. Do NOT touch the `z.enum` role reconciliations in AdminUserEdit/AdminCreateUser — those are Task 2.
  </action>
  <acceptance_criteria>
    - Each listed file imports `can` from `~/auth/permissions`.
    - AdminUserEdit.tsx and AdminCreateUser.tsx `user.role !== 'staff'` guards now call `can(user,'admin')` and preserve their `NoUserContextError` blocks + `/dashboard` redirect targets.
    - `app/layouts/MainLayout.tsx:188` staff nav gate is now `{user && can(user, 'admin') && (...)}`.
    - `grep -rn "user.role === 'staff'\|user.role !== 'staff'" app/layouts app/pages app/middleware` returns NO matches (display-only badges are NOT matched by this pattern — they use `u.role === 'staff'`, a different identifier).
    - All original redirect targets are preserved.
    - `pnpm run typecheck` passes.
  </acceptance_criteria>
  <verify>
    <automated>grep -rn "user.role === 'staff'\|user.role !== 'staff'" app/layouts app/pages app/middleware; test $? -eq 1; pnpm run typecheck</automated>
  </verify>
  <done>All 9 admin/CourseBuilder/guard files + MainLayout migrated to `can(user,'admin')`; no `user.role === 'staff'` or `user.role !== 'staff'` literal remains in app/layouts, app/pages, or app/middleware; typecheck green.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile admin role zod-enums to USER_ROLES (D-04)</name>
  <files>app/pages/admin/AdminUserEdit.tsx, app/pages/admin/AdminCreateUser.tsx</files>
  <read_first>app/.server/database/types.ts, app/pages/admin/AdminUserEdit.tsx, app/pages/admin/AdminCreateUser.tsx</read_first>
  <action>
    In BOTH `app/pages/admin/AdminUserEdit.tsx` (line 43) and `app/pages/admin/AdminCreateUser.tsx` (line 44):
    - Add `import { USER_ROLES } from '~/.server/database/types'` (top of file; note `~/.server` import is safe here because it is used inside a `z.object` schema evaluated at module load on the server — these files are route modules, not client render bodies; this matches existing `.server` usage patterns in the repo).
    - Replace `role: z.enum(['learner', 'staff']),` with `role: z.enum(USER_ROLES),`
    - `USER_ROLES = ['learner', 'staff', 'lite', 'pro'] as const` (verified at app/.server/database/types.ts:25) — this fixes the latent bug where lite/pro roles were rejected by the admin form.
  </action>
  <acceptance_criteria>
    - Both files import `USER_ROLES` from `~/.server/database/types`.
    - Neither file contains `z.enum(['learner', 'staff'])`.
    - `pnpm run typecheck` passes (z.enum(USER_ROLES) is a valid readonly string tuple).
  </acceptance_criteria>
  <verify>
    <automated>grep -rn "z.enum(\['learner', 'staff'\])" app/pages/admin; test $? -eq 1; pnpm run typecheck</automated>
  </verify>
  <done>AdminUserEdit + AdminCreateUser role enums widened to USER_ROLES; no narrow `['learner','staff']` enum remains; typecheck green.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate IndexRedirect + Challenges to can() (D-02, D-04)</name>
  <files>app/pages/IndexRedirect.tsx, app/pages/Challenges.tsx</files>
  <read_first>app/auth/permissions.ts, app/pages/IndexRedirect.tsx, app/pages/Challenges.tsx</read_first>
  <action>
    `app/pages/IndexRedirect.tsx`:
    - Add `import { can } from '~/auth/permissions'`.
    - Replace the loader block:
      ```
      if (user?.role === 'staff') {
          throw redirect('/admin')
      }
      throw redirect('/dashboard')
      ```
      with:
      ```
      if (can(user, 'admin')) {
          throw redirect('/admin')
      }
      throw redirect('/dashboard')
      ```
      (Preserves behavior: staff → /admin, others → /dashboard. `can(null,'admin')` is false so null user falls through to /dashboard.)

    `app/pages/Challenges.tsx`:
    - Replace line 28 import `import { canAccessChallenges } from '~/.server/payment/access'` with `import { can } from '~/auth/permissions'`.
    - Line 82: `const canSubmit = canAccessChallenges(user.role)` → `const canSubmit = can(user, 'accessChallenges')`.
    - Lines 102-105: `if (!canAccessChallenges(user.role)) { return { error: 'Upgrade to Lite or Pro to submit flags.' } }` → `if (!can(user, 'accessChallenges')) { return { error: 'Upgrade to Lite or Pro to submit flags.' } }`.
  </action>
  <acceptance_criteria>
    - `app/pages/IndexRedirect.tsx` no longer contains `user?.role === 'staff'`; imports `can`; staff→/admin, else→/dashboard preserved.
    - `app/pages/Challenges.tsx` no longer imports `canAccessChallenges`; uses `can(user, 'accessChallenges')` in loader (canSubmit) and action (error return).
    - `grep -rn "canAccessChallenges" app/pages` returns NO matches.
    - `pnpm run typecheck` passes.
  </acceptance_criteria>
  <verify>
    <automated>grep -rn "canAccessChallenges" app/pages; test $? -eq 1; pnpm run typecheck</automated>
  </verify>
  <done>IndexRedirect + Challenges migrated to can(); no canAccessChallenges reference remains in app/pages.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| page→can() | Server loaders/actions and client render bodies call can() on resolved user |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Tampering | Admin role enum narrower than DB | medium | mitigate | Task 2 widens AdminUserEdit/AdminCreateUser `z.enum(['learner','staff'])` to `z.enum(USER_ROLES)` so valid lite/pro roles are accepted by admin forms |
| T-01-05 | Elevation of Privilege | Forgotten guard at a new route | medium | mitigate | Central `can(user,'admin')` is now the only pattern; new routes import it. Full migration in this wave removes the scattered literals (app/pages, app/middleware, AND app/layouts MainLayout:188) that were the source of inconsistency |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this phase; slopcheck N/A |
</threat_model>

<verification>
- `pnpm exec vitest run` → full suite green (permissions.test.ts + access.test.ts).
- `pnpm run typecheck` → passes.
- `pnpm run fmt` → clean.
- `grep -rn "user.role === 'staff'\|user.role !== 'staff'" app/layouts app/pages app/middleware` → NO matches (display-only badges use `u.role === 'staff'`, a different identifier, and are out of scope).
- `grep -rn "canAccessChallenges" app` → no matches.
</verification>

<success_criteria>
- Every admin guard, IndexRedirect, CourseBuilder(+Lesson), Challenges, AND MainLayout staff nav gate uses `can()`.
- AdminUserEdit.tsx and AdminCreateUser.tsx `user.role !== 'staff'` guards migrated to `can(user,'admin')` with preserved NoUserContextError + `/dashboard` redirects.
- MainLayout.tsx:188 migrated to `{user && can(user,'admin') && (...)}`.
- Admin role enums accept full USER_ROLES vocabulary.
- `canAccessChallenges` fully removed from page code.
- typecheck + full vitest green; NO `user.role === 'staff'` or `user.role !== 'staff'` literal remains in app/layouts, app/pages, or app/middleware.
</success_criteria>

<output>
Create `.planning/phases/01-role-access-control-implementation/01-02-SUMMARY.md` when done.
</output>
